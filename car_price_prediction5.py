# -*- coding: utf-8 -*-
"""car price prediction5.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1wrHfoKkH6PRd4Ta7kQUM3tf9vnUWbxP6
"""

import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns
from sklearn.preprocessing import LabelEncoder

dataset = pd.read_csv('/content/car_data_formatted.csv')

dataset.head(2)



dataset.isnull().sum()

dataset.info()



#car_name



Car_Name_le = LabelEncoder()
dataset['Car_Name'] = Car_Name_le.fit_transform(dataset["Car_Name"])



#fule_type

dataset['Fuel_Type'].unique()



Fuel_Type_le = LabelEncoder()

dataset['Fuel_Type'] = Car_Name_le.fit_transform(dataset["Fuel_Type"])



#seller type





sellar_type_le = LabelEncoder()

dataset['Seller_Type'] = sellar_type_le.fit_transform(dataset["Seller_Type"])

#tranmissar

Transmission_le = LabelEncoder()

dataset['Transmission'] = Transmission_le.fit_transform(dataset["Transmission"])

dataset.head(3)

sns.heatmap(data=dataset.corr(), annot=True); plt.show()

input_data = dataset.iloc[:,:-1]

output_data = dataset ["Selling_Price"]

from sklearn.preprocessing import StandardScaler

from sklearn.preprocessing import StandardScaler

ss = StandardScaler()
input_data = pd.DataFrame (ss.fit_transform(input_data), columns=input_data.columns)



from sklearn.model_selection import train_test_split

x_train, x_test, y_train, y_test = train_test_split(input_data, output_data, test_size=0.2, random_state=42)



from sklearn.linear_model import LinearRegression, Lasso, Ridge, ElasticNet
from sklearn.tree import DecisionTreeRegressor
from sklearn.svm import SVR
from sklearn.neighbors import KNeighborsRegressor
from sklearn.ensemble import RandomForestRegressor



lr = LinearRegression()

lr.fit(x_train,y_train)

lr.score(x_train, y_train)*100, lr.score(x_test,y_test)*100